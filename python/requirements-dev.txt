# Development and Code Quality Dependencies
# Includes lightweight libraries needed for tests

# Code Quality
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0

# Testing Framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
moto>=4.2.0
httpx>=0.25.0

# AWS SDK (needed for tests)
boto3>=1.34.0
botocore>=1.34.0

# API Framework (needed for tests)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Data Processing (lightweight)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Image Processing (lightweight)
Pillow>=10.0.0
pydicom>=2.4.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
marshmallow>=3.20.0
jsonschema>=4.20.0
structlog>=23.2.0
python-json-logger>=2.0.7
click>=8.1.0
tqdm>=4.66.0

# NOTE: Heavy ML libraries (TensorFlow, PyTorch, SimpleITK, OpenCV) are 
# excluded from dev requirements. Add them only if tests specifically need them.
